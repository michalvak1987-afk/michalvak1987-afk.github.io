# הגדרת הסנכרון — מה שאתה צריך לעשות פעם אחת

הקוד מוכן. חסר רק **מזהה לקוח** מגוגל. עד שיהיה, התוכנה עובדת בדיוק כמו עד היום —
הנתונים בדפדפן, גיבוי לקובץ, הכול כרגיל. הסנכרון מופיע בהגדרות כ"טרם הוגדר".

---

## שלב 1 · יצירת פרויקט

1. היכנס ל־**console.cloud.google.com** עם חשבון הגוגל שלך.
2. למעלה, ליד הלוגו, לחץ על בורר הפרויקטים ← **New Project**.
3. שם: `Bagrut Simulator` ← **Create**.

## שלב 2 · הפעלת Drive API

1. בתפריט הצד: **APIs & Services ← Library**.
2. חפש `Google Drive API` ← **Enable**.

## שלב 3 · מסך ההסכמה

1. **APIs & Services ← OAuth consent screen**.
2. סוג: **External** ← Create.
3. שם האפליקציה: `סימולטור בגרות` · אימייל תמיכה: שלך · אימייל יצירת קשר: שלך.
4. בשלב **Scopes** לחץ **Add or remove scopes**, חפש והוסף בדיוק את זה:

   ```
   https://www.googleapis.com/auth/drive.appdata
   ```

   שים לב שהוא מופיע תחת **Non-sensitive** — זה בכוונה. ההרשאה נותנת גישה
   לתיקייה נסתרת של האפליקציה בלבד, לא לשום קובץ אחר בדרייב.
5. בשלב **Test users** הוסף את כתובות הגוגל שיתחברו (שלך, ושל הרכזות שבודקות).
   **עד 100 משתמשים** אפשר לעבוד כך בלי אימות גוגל. מעבר לזה — צריך לפרסם.

## שלב 4 · מזהה הלקוח

1. **APIs & Services ← Credentials ← Create Credentials ← OAuth client ID**.
2. סוג: **Web application**. שם: `Bagrut Web`.
3. תחת **Authorized JavaScript origins** הוסף **בדיוק** את הכתובת שלך:

   ```
   https://michalvak1987-afk.github.io
   ```

   בלי לוכסן בסוף, בלי `/index.html`. אם תריץ גם מכתובת אחרת — הוסף גם אותה.
4. **Redirect URIs** — להשאיר ריק. אנחנו לא משתמשים בהפניה.
5. **Create**. תקבל מזהה שנראה כך:

   ```
   123456789012-abcdefghijklmnop.apps.googleusercontent.com
   ```

## שלב 5 · הדבקה בקובץ

פתח את `index.html`, חפש את השורה:

```js
const DRIVE_CLIENT_ID='';
```

והדבק בין הגרשיים:

```js
const DRIVE_CLIENT_ID='123456789012-abcdefghijklmnop.apps.googleusercontent.com';
```

זה הכול. העלה לגיטהב, ובהגדרות ← "חשבון וסנכרון בין מכשירים" יופיע
**"חיבור לגוגל דרייב"**.

---

## מה לבדוק אחרי ההגדרה

1. במחשב: חיבור לדרייב ← אישור ההרשאה ← החיווי בכותרת הופך ל־**"מסונכרן"**.
2. שנה ציון. אחרי כ־4 שניות החיווי חוזר ל"מסונכרן".
3. באייפון: אותו קישור, אותו חשבון גוגל ← הציון מופיע.
4. כבה אינטרנט, הזן 10 ציונים, רענן דף ← הנתונים שם והחיווי אומר "ממתין".
5. הדלק אינטרנט ← הכול עולה לבד.

## שאלות שיישאלו אותך

**"האם אתה רואה את הציונים?"** לא. הקובץ בחשבון הדרייב של הרכזת,
בתיקייה נסתרת. אין לי שרת ואין לי גישה.

**"איפה זה בדרייב שלי?"** בתיקיית אפליקציה נסתרת — לא תראי אותה בין
הקבצים. לצפייה או מחיקה: הגדרות דרייב ← ניהול אפליקציות.

**"מה אם אאבד את חשבון הגוגל?"** לכן **הגיבוי לקובץ נשאר חובה**.
הענן הוא רצף בין מכשירים, לא תחליף לגיבוי.

**"האם זה מאושר על ידי משרד החינוך?"** לא. הנתונים יושבים בחשבון הפרטי
של הרכזת. זה כמו לשמור קובץ אקסל ציונים בדרייב האישי — נפוץ מאוד,
אבל לא סביבה מנוהלת של בית הספר. אמור את זה מראש.
